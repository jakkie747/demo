rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all paths for development.
    // This also resolves the CORS issue for uploads.
    // WARNING: This is insecure for production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
