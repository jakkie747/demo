rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access to all documents for this demo.
      // In a production app, you would implement more restrictive rules
      // based on user authentication.
      allow read, write: if true;
    }
  }
}
